streamlit==1.40.1
openai==1.58.1
python-dotenv==1.0.0
httpx==0.27.0
# pandas ships transitively with streamlit, but this app imports it directly,
# so declare it. openpyxl reads the SEC roster when it ships as .xlsx.
# pdfplumber extracts Item 5.F regulatory AUM from each firm's ADV filing —
# AUM is not in any free SEC bulk file, only in the filing itself.
# Left unpinned deliberately: Railway builds on Python 3.14, and pinning older
# releases is what caused the pydantic-core wheel build failure previously.
pandas
openpyxl
pdfplumber
